get:
  responses:
    "200":
      description:
        success
    "401":
      description:
        Invalid request
      content:
        application/json:
          schema:
            $ref: "../../shared/schemas/error.yaml"
    "500":
      description:
        Server error
      content:
        application/json:
          schema:
            $ref: "../../shared/schemas/error.yaml"
